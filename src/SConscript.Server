# vim: ft=python

Import('env')

dir = 'Server'

envCopy = env.Clone()
envCopy.Append(
                CPPPATH = '.'
              )

sourceTargets = [ # nothing to build yet
                  ]

targets = []
for source in sourceTargets:
  targets.append(modelDir + '/' + source)

library = envCopy.Library('Server',targets)

Return( 'library' )
