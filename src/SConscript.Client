# vim: ft=python

Import('env')

dir = 'Client'

envCopy = env.Clone()
envCopy.Append(
                CPPPATH = '.'
              )

sourceTargets = [ # nothing to build yet
                  ]

targets = []
for source in sourceTargets:
  targets.append(modelDir + '/' + source)

program = envCopy.Program('Client',targets)

Return( 'program' )
